# OnePlus 6 (enchilada)

Role: Mobile device

## Hardware Notes
- System partition size: ~2.8 GB (`0xB2C00000`).
- NixOS rootfs size: ~9.4 GB.
- Rootfs MUST be flashed to the `userdata` partition as it does not fit in the `system` partition.
