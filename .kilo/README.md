# Kilo Configuration

This project uses `.opencode` for agentic infrastructure and configuration to maintain compatibility with `openagentcontrol`. 

Refer to `.opencode/opencode.json` for project settings and `.opencode/AGENTS.md` for agent-specific instructions.
